#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
typedef vector<ll> vi;
typedef pair<ll,ll> pi;
typedef vector<pi> vpi;
typedef long double ld;
#define clr(arrr) memset(arrr,-1,sizeof(arrr) )
#define foru(i,N) for(int i=0;i<N;i++)
#define pb push_back
#define mp make_pair

int main()
{int n,l,res,t;
cin>>n>>l;
res=0;
foru(i,n){
cin>>t;
if(t>l)res+=2;
else res+=1;
}

printf("%d",res);

return 0;
}
